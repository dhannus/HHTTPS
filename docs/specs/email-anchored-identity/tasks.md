# Tasks: `email-anchored-identity` (Reihenfolge verbindlich, ein Commit pro Task)

Branch: `claude/kind-pasteur-kweqf1` (von der Umgebung vorgegeben; Rolle des `feat/`-Branches). Diff-Basis für Review: `main...HEAD`.

| # | Ziel | Dateien | AK |
|---|------|---------|----|
| T0 | Test-/Lint-Infrastruktur: `pnpm test` (node --test über `test/`), `pnpm lint` (ESLint 9 Flat-Config), `dotenv` als Dependency, Dev-Transport nur mit vorhandenem sendmail (D8), Integrations-Harness `test/helpers/server.mjs` (startet server.js gegen `TEST_PG_HOST`, wartet auf `/hhttps/info`). Roter Test: Harness-Smoke `/hhttps/info` liefert 200. | `package.json`, `eslint.config.js`, `email.js` (createTransport), `test/helpers/*`, `test/integration/smoke.test.mjs` | — (Infra) |
| T1 | `identity.js`: `normalizeEmail`, `emailAnchorHash(email)`, `sanitizePseudonym`, `generatePseudonym`, `normalizeCode`, `methodFlags(methods)`. Reine Funktionen, unit-getestet. | `identity.js`, `test/unit/identity.test.mjs` | AK-2, AK-6, AK-7, AK-23, AK-24, AK-18 (Flags) |
| T2 | Code-Mail: Code ohne Leerzeichen, `verifyEmailCode` nutzt `normalizeCode`, helle Template-Shell (Theme-Parameter), wahrheitsgemäßer Datenschutzhinweis. Template-Renderer als testbare Funktion `renderVerificationEmail({code,...})` exportieren. | `email.js`, `test/unit/email-template.test.mjs`, `test/integration/email-code.test.mjs` | AK-22, AK-23, AK-24, AK-25, AK-26 |
| T3 | Migration Phase 8 + DB-Layer: `identity_anchors`, `identity_claims_cache`, `sessions.pseudonym`, `authorization_codes.{email,pseudonym,verified_methods}`, `allowed_scopes += email`; `db.identityAnchors.resolveOrCreate`, `db.identityClaimsCache.{upsert,get}`, `db.sessions.update` erlaubt `userId`/`pseudonym`, `db.authCodes.create/claim` mit neuen Feldern (E-Mail beim Claim nullen), `cleanupExpired` räumt Cache. Migration wird beim Boot idempotent ausgeführt (wie `ensureCodeColumn`) UND liegt als SQL-Datei vor. | `sql/migration-phase-8-email-anchored-identity.sql`, `db.js`, `test/integration/db-phase8.test.mjs` | AK-1, AK-16 (Persistenz), AK-17 (Löschen beim Claim) |
| T4 | Anker-Auflösung bei E-Mail-Bestätigung: `/hhttps/email/send` nimmt `pseudonym` entgegen (auf Session gespeichert), `/hhttps/email/confirm-code` und `/hhttps/email/verify` binden Session an Anker (`userId`, `pseudonym`), schreiben Cache; Antwort enthält `userId`, `pseudonym`. | `server.js`, `test/integration/email-anchor.test.mjs` | AK-1, AK-2, AK-6, AK-7, AK-8, AK-16 |
| T5 | E-Mail-zuerst-Gate + Passkey-Handle: `requireEmailVerified`; `webauthn/register/start` mit `sessionId` (user handle = session.userId); Gates in `verify/github/start`, `eid/upgrade`, `role/declare`; `role/declare`/`eid/upgrade`/`token/refresh` tragen `pseudonym` + Methoden-Flags im Token. | `server.js`, `test/integration/gate.test.mjs` | AK-4, AK-5, AK-9, AK-10, AK-11, AK-12, AK-13 |
| T6 | OAuth-Übertragung: Scope `email` (SCOPES_KNOWN, Discovery, Consent-Label, Default-allowed), `approve` liest Token-Methoden + Cache → Code-Zeile; `token` (code + refresh) und `userinfo` liefern `verified_methods`, `*_verified`, `preferred_username`, `email` (Scope). | `server.js`, `test/integration/oauth-claims.test.mjs` | AK-3, AK-17, AK-18, AK-19, AK-20, AK-21 |
| T7 | Sign-in-Seite: Pseudonym-Feld im E-Mail-Panel, Methoden-Buttons disabled bis E-Mail bestätigt, `register/start` mit `sessionId`, Pseudonym-Anzeige nach Bestätigung, i18n-Texte DE/EN. Test: statische Prüfung des HTML (Attribute/Strings) per node-Test. | `public/index.html`, `test/unit/signin-page.test.mjs` | AK-14, AK-15 |

Gates nach jedem Task: `cd server && pnpm test && pnpm lint` (Integrationstests laufen mit `TEST_PG_HOST=/var/lib/pgtest`).
